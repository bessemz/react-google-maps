// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Cette couleur est juste un exemple, vous pouvez la remplacer
    },
    secondary: {
      main: '#ff9100', // Cette couleur est juste un exemple, vous pouvez la remplacer
    },
    // Vous pouvez ajouter d'autres configurations ici
  },
  typography: {
    // Définir la typographie ici si nécessaire
  },
  // ... d'autres options de thème si vous en avez besoin
});

export default theme;
